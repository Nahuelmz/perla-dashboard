# Clientes P4 + P5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar P5 (Perla Status Panel accesible via click en logo) y P4 (Necesitan atención en Clientes con sort ambient + filter chip) en el dashboard Perla.

**Architecture:** Dos features independientes que comparten el principio "ambient → urgent". P5 agrega un componente popover/bottom-sheet reusable disparado por el wordmark. P4 extiende la lógica CRM de Clientes con un helper puro de estado + cambios de sort/filter sin agregar paneles permanentes.

**Tech Stack:** React 18, TypeScript, Tailwind v4, @phosphor-icons/react, date-fns, Vite. Sin framer-motion (bug conocido). Detección mobile via `window.matchMedia`. Animations via CSS keyframes en `index.css`.

**Context operacional:**
- Proyecto en `/Users/nahuelmz/El Nudo/AUN/Nahuel Dashboard/dashboard_v53`.
- Dev server corre en `http://localhost:5176` (Vite HMR).
- NO correr `npm run build` — verificación es visual en browser + HMR.
- NO agregar Co-Authored-By ni AI attribution en commits. Conventional commits format.
- Commits solo cuando el usuario los confirma explícitamente.

**Spec:** `docs/superpowers/specs/2026-04-23-clientes-p4-p5-design.md`

---

## File Structure

### P5 — Perla Status Panel

| Archivo | Responsabilidad |
|---|---|
| `src/components/generated/mockData.ts` | Agregar `agentStatus`, `agentDailyStats`, `agentActivity` |
| `src/components/dashboard/PerlaStatusPanel.tsx` | **Nuevo.** Popover desktop + bottom sheet mobile. Un solo componente con detección de viewport. Recibe `open`, `onClose`, `onOpenBotSettings`, data props |
| `src/components/dashboard/PerlaWordmark.tsx` | Agregar prop `onWordmarkClick`; cuando presente, texto + dot disparan el mismo handler |
| `src/components/dashboard/index.ts` | Export `PerlaStatusPanel` |
| `src/index.css` | Agregar keyframe `slideUpSheet` + clase `.anim-slide-up-sheet` |
| `src/components/generated/SalonAdminDashboard.tsx` | State `agentPanelOpen`, cablear `onWordmarkClick` en topbar, render `<PerlaStatusPanel>` |

### P4 — Clientes Necesitan atención

| Archivo | Responsabilidad |
|---|---|
| `src/components/generated/mockData.ts` | Helper puro `getClientAttentionState(client, appointments, now)` |
| `src/components/generated/SalonAdminDashboard.tsx` | (1) Extender `clientStats` con `attentionState`. (2) Modificar sort. (3) Tag inline en item de lista. (4) Agregar chip "Necesitan atención (N)". (5) Extender filter state con `attentionOnly` |

---

## Ordering rationale

P5 primero (5 tasks): menos riesgo de romper features existentes, valida el patrón popover/sheet que podría reusarse en el futuro.

P4 después (4 tasks): toca lógica de negocio + UI de Clientes que ya fue recientemente refactorizada — queremos que P5 ya esté estable antes.

Cada feature termina en un commit separado para rollback granular.

---

# P5 — Perla Status Panel

---

### Task 1: Agregar mock data del agente

**Files:**
- Modify: `src/components/generated/mockData.ts` (agregar al final, antes del `export default` si existe, o al final del export inline)

- [ ] **Step 1: Agregar tipos y data mock**

Agregar al final de `mockData.ts`:

```ts
// ──────────────────────────────────────────────────────────
// Agent (Perla) status + activity — P5 Panel
// ──────────────────────────────────────────────────────────

export type AgentState = 'active' | 'paused';

export type AgentActivityType =
  | 'confirm'
  | 'schedule'
  | 'reschedule'
  | 'cancel'
  | 'reply';

export type AgentActivityItem = {
  id: string;
  type: AgentActivityType;
  label: string;     // e.g. "Confirmó turno de Sofía"
  timeAgo: string;   // e.g. "2h", "ayer", "hace 3h"
};

export const agentStatus: { state: AgentState; since: Date; subtitle: string } = {
  state: 'active',
  since: new Date(Date.now() - 1000 * 60 * 60 * 26), // 26h ago
  subtitle: 'perla está respondiendo',
};

export const agentDailyStats = {
  turnos: 12,
  mensajes: 28,
  confirmRate: 94, // percent, integer
};

export const agentActivity: AgentActivityItem[] = [
  { id: 'a1', type: 'confirm',    label: 'Confirmó turno de Sofía',    timeAgo: '2h' },
  { id: 'a2', type: 'schedule',   label: 'Agendó a Matías (nuevo)',    timeAgo: '5h' },
  { id: 'a3', type: 'reply',      label: 'Respondió a 4 consultas',    timeAgo: '7h' },
  { id: 'a4', type: 'reschedule', label: 'Reagendó turno de Lucía',    timeAgo: 'ayer' },
];
```

- [ ] **Step 2: Verificar en dev server**

Navegar a `http://localhost:5176`, verificar que no hay errores en consola, que el dashboard sigue renderizando normalmente (cambio aditivo, no debería afectar nada).

**No commit todavía** — este task es solo data, se commitea junto con el próximo.

---

### Task 2: Agregar keyframe `slideUpSheet` para bottom sheet

**Files:**
- Modify: `src/index.css` (agregar al final, junto con los otros keyframes)

- [ ] **Step 1: Agregar keyframe y clase**

Agregar al final de `src/index.css`, después de `.anim-float-in`:

```css
@keyframes slideUpSheet {
  from { opacity: 0; transform: translateY(100%); }
  to   { opacity: 1; transform: translateY(0); }
}

.anim-slide-up-sheet {
  animation: slideUpSheet 0.28s cubic-bezier(0.32, 0.72, 0, 1) both;
}

@keyframes fadeInBackdrop {
  from { opacity: 0; }
  to   { opacity: 1; }
}

.anim-fade-backdrop {
  animation: fadeInBackdrop 0.2s ease-out both;
}
```

- [ ] **Step 2: Verificar HMR**

En browser, abrir devtools → elements → verificar que el CSS se recargó sin errores.

**No commit todavía.**

---

### Task 3: Crear componente `PerlaStatusPanel`

**Files:**
- Create: `src/components/dashboard/PerlaStatusPanel.tsx`

- [ ] **Step 1: Crear el archivo con el componente completo**

```tsx
import React, { useEffect, useRef, useState } from 'react';
import { XIcon } from '@phosphor-icons/react';
import type { AgentActivityItem, AgentState } from '@/components/generated/mockData';

const DM_SERIF = "'DM Serif Display', ui-serif, Georgia, serif";
const DM_SANS = "'DM Sans', ui-sans-serif, system-ui, sans-serif";
const JETBRAINS = "'JetBrains Mono', ui-monospace, monospace";

const T = {
  text: '#1B2D3B',
  text2: '#3F5565',
  text3: '#7A8B99',
  border: 'rgba(27,45,59,0.08)',
  orange: '#4472C4',
  sage: '#4472C4',
};

type PerlaStatusPanelProps = {
  open: boolean;
  onClose: () => void;
  anchorRef: React.RefObject<HTMLElement | null>;
  onOpenBotSettings?: () => void;
  status: { state: AgentState; subtitle: string };
  stats: { turnos: number; mensajes: number; confirmRate: number };
  activity: AgentActivityItem[];
};

function useIsMobile(breakpoint = 767) {
  const [isMobile, setIsMobile] = useState(() => {
    if (typeof window === 'undefined') return false;
    return window.matchMedia(`(max-width: ${breakpoint}px)`).matches;
  });
  useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${breakpoint}px)`);
    const handler = (e: MediaQueryListEvent) => setIsMobile(e.matches);
    mq.addEventListener('change', handler);
    return () => mq.removeEventListener('change', handler);
  }, [breakpoint]);
  return isMobile;
}

export function PerlaStatusPanel({
  open,
  onClose,
  anchorRef,
  onOpenBotSettings,
  status,
  stats,
  activity,
}: PerlaStatusPanelProps) {
  const isMobile = useIsMobile();
  const panelRef = useRef<HTMLDivElement>(null);

  // Close on Escape
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [open, onClose]);

  // Desktop: close on outside click (but not clicks on the anchor itself — anchor toggles)
  useEffect(() => {
    if (!open || isMobile) return;
    const onClick = (e: MouseEvent) => {
      const target = e.target as Node;
      if (panelRef.current?.contains(target)) return;
      if (anchorRef.current?.contains(target)) return;
      onClose();
    };
    // Defer listener install by one tick so the opening click doesn't trigger it
    const timer = window.setTimeout(() => {
      document.addEventListener('mousedown', onClick);
    }, 0);
    return () => {
      window.clearTimeout(timer);
      document.removeEventListener('mousedown', onClick);
    };
  }, [open, isMobile, anchorRef, onClose]);

  if (!open) return null;

  const isActive = status.state === 'active';

  const Content = (
    <>
      {/* Header: status */}
      <div style={{ padding: '16px 18px 14px', borderBottom: `1px solid ${T.border}` }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
          <span
            style={{
              width: 8,
              height: 8,
              borderRadius: '50%',
              background: isActive ? T.sage : '#F57C00',
              boxShadow: isActive ? `0 0 0 3px ${T.sage}22` : 'none',
              animation: isActive ? 'sagePulse 2.4s ease-in-out infinite' : 'none',
              display: 'inline-block',
            }}
          />
          <span style={{ fontFamily: DM_SANS, fontSize: 13, fontWeight: 500, color: T.text }}>
            {isActive ? 'Activo' : 'Pausado'}
          </span>
        </div>
        <p style={{ margin: '6px 0 0', fontFamily: DM_SANS, fontSize: 12, color: T.text3 }}>
          {status.subtitle}
        </p>
      </div>

      {/* KPIs */}
      <div
        style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(3, 1fr)',
          gap: 0,
          padding: '18px 10px',
          borderBottom: `1px solid ${T.border}`,
        }}
      >
        {[
          { value: stats.turnos.toString(), label: 'turnos' },
          { value: stats.mensajes.toString(), label: 'msjs' },
          { value: `${stats.confirmRate}%`, label: 'confirm.' },
        ].map((kpi) => (
          <div key={kpi.label} style={{ textAlign: 'center' }}>
            <div
              style={{
                fontFamily: DM_SERIF,
                fontSize: 26,
                lineHeight: 1,
                color: T.text,
                fontVariantNumeric: 'tabular-nums',
              }}
            >
              {kpi.value}
            </div>
            <div
              style={{
                marginTop: 6,
                fontFamily: DM_SANS,
                fontSize: 9.5,
                textTransform: 'uppercase',
                letterSpacing: '0.12em',
                color: T.text3,
              }}
            >
              {kpi.label}
            </div>
          </div>
        ))}
      </div>

      {/* Activity */}
      <div style={{ padding: '14px 18px' }}>
        <div
          style={{
            fontFamily: DM_SANS,
            fontSize: 10,
            textTransform: 'uppercase',
            letterSpacing: '0.12em',
            color: T.text3,
            marginBottom: 10,
          }}
        >
          Actividad reciente
        </div>
        <ul style={{ listStyle: 'none', margin: 0, padding: 0 }}>
          {activity.map((item) => (
            <li
              key={item.id}
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'baseline',
                gap: 10,
                padding: '6px 0',
                fontFamily: DM_SANS,
                fontSize: 12.5,
                color: T.text2,
              }}
            >
              <span style={{ flex: 1, minWidth: 0, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                {item.label}
              </span>
              <span
                style={{
                  fontFamily: JETBRAINS,
                  fontSize: 10.5,
                  color: T.text3,
                  flexShrink: 0,
                }}
              >
                {item.timeAgo}
              </span>
            </li>
          ))}
        </ul>
      </div>

      {/* Footer link */}
      {onOpenBotSettings && (
        <button
          onClick={() => {
            onClose();
            onOpenBotSettings();
          }}
          style={{
            width: '100%',
            borderTop: `1px solid ${T.border}`,
            padding: '12px 18px',
            background: 'transparent',
            border: 'none',
            textAlign: 'left',
            fontFamily: DM_SANS,
            fontSize: 12,
            color: T.text3,
            cursor: 'pointer',
            transition: 'color 0.15s, background 0.15s',
          }}
          onMouseEnter={(e) => {
            e.currentTarget.style.color = T.text;
            e.currentTarget.style.background = 'rgba(27,45,59,0.03)';
          }}
          onMouseLeave={(e) => {
            e.currentTarget.style.color = T.text3;
            e.currentTarget.style.background = 'transparent';
          }}
        >
          Configurar agente →
        </button>
      )}
    </>
  );

  if (isMobile) {
    return (
      <>
        {/* Backdrop */}
        <div
          className="anim-fade-backdrop"
          onClick={onClose}
          style={{
            position: 'fixed',
            inset: 0,
            background: 'rgba(27,45,59,0.32)',
            zIndex: 60,
          }}
        />
        {/* Sheet */}
        <div
          ref={panelRef}
          className="anim-slide-up-sheet"
          role="dialog"
          aria-label="Estado del agente Perla"
          style={{
            position: 'fixed',
            bottom: 0,
            left: 0,
            right: 0,
            maxHeight: '75vh',
            overflowY: 'auto',
            background: '#ffffff',
            borderTopLeftRadius: 20,
            borderTopRightRadius: 20,
            boxShadow: '0 -8px 40px rgba(27,45,59,0.18)',
            zIndex: 61,
            paddingBottom: 'env(safe-area-inset-bottom, 8px)',
          }}
        >
          {/* Grab handle */}
          <div style={{ display: 'flex', justifyContent: 'center', paddingTop: 8 }}>
            <div style={{ width: 36, height: 4, borderRadius: 2, background: 'rgba(27,45,59,0.18)' }} />
          </div>
          {/* Close button */}
          <button
            onClick={onClose}
            aria-label="Cerrar"
            style={{
              position: 'absolute',
              top: 10,
              right: 12,
              background: 'transparent',
              border: 'none',
              padding: 6,
              cursor: 'pointer',
              color: T.text3,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <XIcon size={18} weight="regular" />
          </button>
          {Content}
        </div>
      </>
    );
  }

  // Desktop popover
  return (
    <div
      ref={panelRef}
      className="anim-float-in"
      role="dialog"
      aria-label="Estado del agente Perla"
      style={{
        position: 'absolute',
        top: 'calc(100% + 12px)',
        left: 0,
        width: 320,
        background: '#ffffff',
        borderRadius: 14,
        border: `1px solid ${T.border}`,
        boxShadow: '0 8px 40px rgba(27,45,59,0.14), 0 1px 3px rgba(27,45,59,0.06)',
        zIndex: 40,
        overflow: 'hidden',
      }}
    >
      {Content}
    </div>
  );
}

export default PerlaStatusPanel;
```

- [ ] **Step 2: Verificar que compila (sin correr build)**

Observar el browser dev console — si hay errores de TS de Vite aparecerán ahí. Esperado: sin errores. El componente aún no se renderiza en ningún lado.

**No commit todavía.**

---

### Task 4: Exportar `PerlaStatusPanel` desde dashboard/index.ts

**Files:**
- Modify: `src/components/dashboard/index.ts`

- [ ] **Step 1: Agregar export**

Agregar al final del archivo (antes de cualquier línea vacía final):

```ts
export { PerlaStatusPanel } from './PerlaStatusPanel';
```

- [ ] **Step 2: Verificar en browser**

Sin errores en consola. No hay cambio visual aún.

**No commit todavía.**

---

### Task 5: Extender `PerlaWordmark` con `onWordmarkClick`

**Files:**
- Modify: `src/components/dashboard/PerlaWordmark.tsx`

- [ ] **Step 1: Agregar prop al type**

Cambiar el type `PerlaWordmarkProps`:

```tsx
type PerlaWordmarkProps = {
  size?: 'sm' | 'md' | 'lg';
  color?: string;
  dotColor?: string;
  pulse?: boolean;
  onDotClick?: () => void;
  onWordmarkClick?: () => void;  // ← nueva
  dotTitle?: string;
  dotTooltip?: React.ReactNode;
  className?: string;
};
```

- [ ] **Step 2: Desestructurar la prop nueva**

En la firma del componente, agregar `onWordmarkClick`:

```tsx
export function PerlaWordmark({
  size = 'md',
  color = '#1B2D3B',
  dotColor = '#4472C4',
  pulse = false,
  onDotClick,
  onWordmarkClick,
  dotTitle,
  dotTooltip,
  className,
}: PerlaWordmarkProps) {
```

- [ ] **Step 3: Hacer clickeable el contenedor cuando hay `onWordmarkClick`**

Reemplazar el `<div>` wrapper completo (el que envuelve `<span>perla</span>` y el dot). Cambiar de:

```tsx
<div
  className={className}
  style={{
    display: 'inline-flex',
    alignItems: 'baseline',
    gap: '2px',
    userSelect: 'none' as const,
    lineHeight: 1,
  }}
>
```

A:

```tsx
<div
  className={className}
  onClick={onWordmarkClick ? (e) => {
    // Only fire if the click didn't originate on the dot button
    // (dot button has its own stopPropagation via button element)
    onWordmarkClick();
  } : undefined}
  style={{
    display: 'inline-flex',
    alignItems: 'baseline',
    gap: '2px',
    userSelect: 'none' as const,
    lineHeight: 1,
    cursor: onWordmarkClick ? 'pointer' : 'default',
  }}
>
```

**Nota:** cuando hay tanto `onWordmarkClick` como `onDotClick`, ambos disparan — el usuario puede querer que los dos hagan lo mismo (ej. abrir el panel). Si `onDotClick` es diferente de `onWordmarkClick`, el click en el dot dispara ambos (el botón no tiene stopPropagation). Para P5 esto es el comportamiento deseado: tocar el dot = tocar el wordmark = abrir el panel. Pero si el caller pasa solo `onWordmarkClick` (sin `onDotClick`), el span del dot no es clickeable — solo el wrapper recibe el click. Perfecto.

- [ ] **Step 4: Verificar en browser**

Abrir `http://localhost:5176`, navegar al login screen (donde aparece el wordmark). El comportamiento actual no cambia (login no pasa `onWordmarkClick`). Sin errores en consola.

**No commit todavía.**

---

### Task 6: Cablear `PerlaStatusPanel` en `SalonAdminDashboard`

**Files:**
- Modify: `src/components/generated/SalonAdminDashboard.tsx`

- [ ] **Step 1: Agregar imports**

En la sección de imports del archivo (junto con los otros imports de `@/components/dashboard`):

```tsx
import { PerlaStatusPanel } from '@/components/dashboard';
```

Y de mockData (buscar la línea existente `import { ... } from './mockData'` y agregar):

```tsx
import { agentStatus, agentDailyStats, agentActivity } from './mockData';
```

(Si ya hay un import from `./mockData`, agregar los nombres a ese import existente.)

- [ ] **Step 2: Agregar state para el panel y ref para el wordmark**

Buscar el componente principal (el que renderiza la topbar con el `PerlaWordmark`). Cerca de los otros `useState` agregar:

```tsx
const [agentPanelOpen, setAgentPanelOpen] = useState(false);
const agentAnchorRef = useRef<HTMLDivElement>(null);
```

Asegurarse que `useRef` está importado de `react`.

- [ ] **Step 3: Cablear el wordmark en la topbar**

Buscar el `<PerlaWordmark ... />` de la topbar (NO el del LoginScreen — ese no se toca). Envolverlo en un div con `ref` y pasarle `onWordmarkClick`:

```tsx
<div ref={agentAnchorRef} style={{ position: 'relative' }}>
  <PerlaWordmark
    size="md"
    color={T.text}
    dotColor={agentStatus.state === 'active' ? T.orange : '#F57C00'}
    pulse={agentStatus.state === 'active'}
    onWordmarkClick={() => setAgentPanelOpen((v) => !v)}
    dotTitle={agentStatus.state === 'active' ? 'Agente activo' : 'Agente pausado'}
  />
  <PerlaStatusPanel
    open={agentPanelOpen}
    onClose={() => setAgentPanelOpen(false)}
    anchorRef={agentAnchorRef}
    onOpenBotSettings={() => setBotSettingsOpen(true)}
    status={agentStatus}
    stats={agentDailyStats}
    activity={agentActivity}
  />
</div>
```

**Nota:** el nombre `setBotSettingsOpen` asume que hay state existente para el `BotSettingsModal`. Si el state tiene otro nombre (buscar en el archivo con Grep `BotSettingsModal`), usar ese nombre. Si no existe o está wired diferente, pasar `onOpenBotSettings={undefined}` por ahora — el link del footer no se renderiza cuando la prop es falsy.

- [ ] **Step 4: Verificar visualmente en desktop**

1. Abrir `http://localhost:5176` en desktop (viewport ≥ 768px).
2. Click en el wordmark "perla•" de la topbar.
3. **Esperado:** popover aparece anclado debajo del wordmark, ancho 320px, con status + KPIs + actividad + link "Configurar agente →".
4. Click afuera del popover → cierra.
5. Click de nuevo en el wordmark → abre.
6. Con el popover abierto, presionar `Escape` → cierra.
7. Click en "Configurar agente →" → abre `BotSettingsModal` (si está wired).

- [ ] **Step 5: Verificar visualmente en mobile**

1. Devtools → toggle device toolbar → preset iPhone 14 (390×844).
2. Hard reload.
3. Click en el wordmark "perla•" del topbar mobile.
4. **Esperado:** sheet desliza desde abajo con grab handle arriba, ocupa full width, max 75vh.
5. Click en backdrop oscuro → cierra.
6. Click en botón X arriba-derecha → cierra.

- [ ] **Step 6: Commit P5**

Si el usuario aprueba, commit:

```bash
git add src/components/dashboard/PerlaWordmark.tsx \
        src/components/dashboard/PerlaStatusPanel.tsx \
        src/components/dashboard/index.ts \
        src/components/generated/mockData.ts \
        src/components/generated/SalonAdminDashboard.tsx \
        src/index.css
git commit -m "feat(dashboard): add Perla status panel (popover desktop + bottom sheet mobile)"
```

---

# P4 — Necesitan atención en Clientes

---

### Task 7: Agregar helper `getClientAttentionState` en mockData

**Files:**
- Modify: `src/components/generated/mockData.ts`

- [ ] **Step 1: Agregar el tipo y el helper**

Al final de `mockData.ts`, después del bloque del agente:

```ts
// ──────────────────────────────────────────────────────────
// Clientes — Attention State (P4)
// ──────────────────────────────────────────────────────────

export type ClientAttentionState = 'activo' | 'en_riesgo' | 'inactivo';

/**
 * Determines the attention state of a client based on their appointment history.
 *
 * Rules:
 * - activo:    had a confirmed appointment in the last 30 days
 * - en_riesgo: last confirmed appointment is 31–60 days ago, OR had ≥1 no-show in last 60 days
 * - inactivo:  no appointments in the last 60 days, or never attended
 */
export function getClientAttentionState(
  clientId: string,
  appointments: Array<{ clientId: string; date: Date; status: string }>,
  now: Date = new Date()
): ClientAttentionState {
  const DAY_MS = 86_400_000;
  const daysSince = (d: Date) => Math.floor((now.getTime() - d.getTime()) / DAY_MS);

  const clientApps = appointments.filter((a) => a.clientId === clientId);
  if (clientApps.length === 0) return 'inactivo';

  const confirmedPast = clientApps
    .filter((a) => a.status === 'confirmed' && a.date <= now)
    .sort((a, b) => b.date.getTime() - a.date.getTime());

  const noShowsRecent = clientApps.filter(
    (a) => a.status === 'no_show' && daysSince(a.date) <= 60
  );

  const lastConfirmed = confirmedPast[0];

  if (lastConfirmed) {
    const days = daysSince(lastConfirmed.date);
    if (days <= 30 && noShowsRecent.length === 0) return 'activo';
    if (days <= 60 || noShowsRecent.length >= 1) return 'en_riesgo';
    return 'inactivo';
  }

  // No confirmed history, but has appointments (e.g. only no-shows / cancellations)
  if (noShowsRecent.length >= 1) return 'en_riesgo';
  return 'inactivo';
}
```

- [ ] **Step 2: Verificar HMR**

En browser, verificar que no hay errores. Cambio puramente aditivo.

**No commit todavía.**

---

### Task 8: Extender `clientStats` con `attentionState` y modificar sort

**Files:**
- Modify: `src/components/generated/SalonAdminDashboard.tsx`

- [ ] **Step 1: Importar el helper**

Buscar el import de `./mockData` existente y agregar `getClientAttentionState` y el tipo:

```tsx
import { getClientAttentionState, type ClientAttentionState } from './mockData';
```

- [ ] **Step 2: Extender el `useMemo` de `clientStats`**

Buscar el `useMemo` que calcula `clientStats` (contiene `nextApp`, `lastApp`, `confirmedCount`, etc). Dentro del `.map(c => { ... })`, agregar al objeto retornado:

```tsx
const attentionState = getClientAttentionState(c.id, appointments, new Date());
const hasNoShow = apps.some(
  (a) => a.status === 'no_show' &&
  (new Date().getTime() - a.date.getTime()) / 86_400_000 <= 60
);
return {
  ...c,
  // ... campos existentes ...
  attentionState,
  hasNoShow,
};
```

**Importante:** mantener todos los campos existentes del return original (`nextApp`, `lastApp`, `confirmedCount`, `hasUpcoming`, etc.). Solo **agregar** `attentionState` y `hasNoShow`.

- [ ] **Step 3: Modificar el sort de la lista de Clientes**

Buscar el sort (puede estar en el mismo useMemo o en uno separado que ordena `clientStats`). Prependear la nueva prioridad:

```tsx
const attentionOrder = (s: ClientAttentionState) =>
  s === 'en_riesgo' ? 0 : s === 'activo' ? 1 : 2;

// ... dentro del .sort((a, b) => { ... })
const byAttention = attentionOrder(a.attentionState) - attentionOrder(b.attentionState);
if (byAttention !== 0) return byAttention;
// ... resto del sort existente (nextApp, lastApp, alfabético) ...
```

- [ ] **Step 4: Verificar en browser**

Navegar a Clientes. **Esperado:** ningún cambio visual todavía (el sort ya surface casos `en_riesgo` pero sin tag aún no se distinguen). Sin errores en consola.

**No commit todavía.**

---

### Task 9: Agregar tag inline con jerarquía en el item de lista

**Files:**
- Modify: `src/components/generated/SalonAdminDashboard.tsx`

- [ ] **Step 1: Crear helper para decidir qué tag mostrar**

Cerca del top del componente `ClientsScreen` (o dentro, antes del return), agregar:

```tsx
type ClientTag = { label: string; tone: 'orange' | 'sage' | 'muted' };

function getClientTag(client: {
  attentionState: ClientAttentionState;
  hasNoShow: boolean;
  tags?: string[]; // existentes: 'vip', 'frecuente'
}): ClientTag | null {
  // Priority: no-show > en_riesgo > vip > frecuente > inactivo
  if (client.hasNoShow) return { label: 'no-show', tone: 'orange' };
  if (client.attentionState === 'en_riesgo') return { label: 'en riesgo', tone: 'orange' };
  if (client.tags?.includes('vip')) return { label: 'vip', tone: 'sage' };
  if (client.tags?.includes('frecuente')) return { label: 'frecuente', tone: 'sage' };
  if (client.attentionState === 'inactivo') return { label: 'inactivo', tone: 'muted' };
  return null;
}
```

**Nota:** adaptar el campo `client.tags?.includes('vip')` según cómo estén representados los tags en el modelo actual. Si son flags booleanas (`client.isVip`), ajustar. Buscar en el archivo cómo se acceden hoy los tags vip/frecuente y usar la misma forma.

- [ ] **Step 2: Reemplazar el render de tags en el item**

En el JSX del item de Clientes, donde se renderizan los tags `vip` / `frecuente` hoy (buscar por "vip" en el archivo), reemplazar por:

```tsx
{(() => {
  const tag = getClientTag({
    attentionState: c.attentionState,
    hasNoShow: c.hasNoShow,
    tags: c.tags, // ajustar si el shape es distinto
  });
  if (!tag) return null;
  const colorMap = {
    orange: { bg: 'rgba(255,148,77,0.08)', color: T.orange, border: 'rgba(255,148,77,0.25)' },
    sage:   { bg: 'rgba(68,114,196,0.08)', color: T.orange, border: 'rgba(68,114,196,0.22)' },
    muted:  { bg: 'transparent',            color: T.text3,  border: 'transparent' },
  }[tag.tone];
  return (
    <span
      style={{
        fontFamily: "'DM Sans', ui-sans-serif, system-ui, sans-serif",
        fontSize: 10,
        fontWeight: 500,
        padding: '2px 8px',
        borderRadius: 6,
        background: colorMap.bg,
        color: colorMap.color,
        border: `1px solid ${colorMap.border}`,
        letterSpacing: '0.02em',
        whiteSpace: 'nowrap',
      }}
    >
      {tag.label}
    </span>
  );
})()}
```

- [ ] **Step 3: Verificar en browser**

Navegar a Clientes. **Esperado:**
- Clientes con no-show reciente muestran `[ no-show ]` en tono naranja.
- Clientes en riesgo (sin no-show) muestran `[ en riesgo ]` en tono naranja.
- VIPs sin riesgo muestran `[ vip ]`.
- Los clientes con `en_riesgo` aparecen primero en la lista.
- Sin errores en consola.

**No commit todavía.**

---

### Task 10: Agregar chip "Necesitan atención (N)" + filter logic

**Files:**
- Modify: `src/components/generated/SalonAdminDashboard.tsx`

- [ ] **Step 1: Extender el filter state**

Buscar el useState del filter en `ClientsScreen`. Agregar campo `attentionOnly`:

```tsx
const [filterState, setFilterState] = useState<{
  status: 'all' | 'vip' | 'frecuente' | /* otros ya existentes */;
  search: string;
  attentionOnly: boolean;
}>({ status: 'all', search: '', attentionOnly: false });
```

Si el state ya es un shape distinto (varios useStates separados), agregar el nuevo useState:

```tsx
const [attentionOnly, setAttentionOnly] = useState(false);
```

Y ajustar las referencias en los siguientes pasos.

- [ ] **Step 2: Computar count de atención**

En el cuerpo del componente, después del `clientStats`:

```tsx
const attentionCount = clientStats.filter((c) => c.attentionState === 'en_riesgo').length;
```

- [ ] **Step 3: Extender el filter pipeline con la condición AND**

Buscar el `useMemo` que aplica los filtros (o el `.filter(...)` en el render). Agregar:

```tsx
if (attentionOnly && c.attentionState !== 'en_riesgo') return false;
```

A la cadena de condiciones de filtrado existente (ubicarlo donde está la lógica de search + status).

- [ ] **Step 4: Agregar el chip al UI de filtros**

Buscar el container donde viven los chips `Todos / VIP / Frecuente`. Al final de esos chips, agregar condicionalmente:

```tsx
{attentionCount > 0 && (
  <button
    onClick={() => setAttentionOnly((v) => !v)}
    style={{
      fontFamily: "'DM Sans', ui-sans-serif, system-ui, sans-serif",
      fontSize: 12,
      fontWeight: 500,
      padding: '6px 12px',
      borderRadius: 999,
      background: attentionOnly ? 'rgba(255,148,77,0.12)' : 'transparent',
      color: attentionOnly ? T.orange : T.text2,
      border: `1px solid rgba(255,148,77,0.25)`,
      cursor: 'pointer',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      transition: 'background 0.15s, color 0.15s',
      fontVariantNumeric: 'tabular-nums',
    }}
  >
    Necesitan atención
    <span style={{ color: T.orange, fontWeight: 600 }}>({attentionCount})</span>
  </button>
)}
```

Si se usa el setter del state compuesto, reemplazar `setAttentionOnly((v) => !v)` por `setFilterState((s) => ({ ...s, attentionOnly: !s.attentionOnly }))`.

- [ ] **Step 5: Verificar en browser**

1. Navegar a Clientes.
2. **Esperado:** si hay clientes `en_riesgo` en mockData, aparece el chip "Necesitan atención (N)" con borde naranja sutil.
3. Click en el chip → fondo se llena naranja claro, la lista filtra solo a los en riesgo.
4. Segundo click → el chip se apaga, la lista vuelve a mostrar todos (con el sort que prioriza en_riesgo).
5. Click en "VIP" + "Necesitan atención" a la vez → lista muestra solo VIPs que están en riesgo (intersección AND).
6. Si no hay clientes `en_riesgo`, el chip **no aparece**.
7. Sin errores en consola.

- [ ] **Step 6: Verificar mobile**

Devtools → iPhone 14 preset → hard reload. El chip debe caber en la fila de filtros sin romper el layout. Si la fila scrollea horizontalmente, debe hacerlo smooth sin cortar ningún chip.

- [ ] **Step 7: Commit P4**

Si el usuario aprueba:

```bash
git add src/components/generated/mockData.ts \
        src/components/generated/SalonAdminDashboard.tsx
git commit -m "feat(clientes): add attention state sort + filter chip + inline tags"
```

---

## Verificación final end-to-end

Correr después de ambos commits, desde browser:

- [ ] **P5 Desktop (≥1024px):** click wordmark → popover. Click afuera / Escape → cierra. "Configurar agente" → abre `BotSettingsModal`.
- [ ] **P5 Mobile (390px):** click wordmark → bottom sheet. Backdrop / X → cierra. Grab handle visible.
- [ ] **P4 Sort:** primer cliente de la lista es `en_riesgo` (si existen). Último tier es `inactivo`.
- [ ] **P4 Tag jerarquía:** no-show > en_riesgo > vip > frecuente > inactivo. Solo UN tag por cliente.
- [ ] **P4 Chip:** aparece solo con count > 0. Toggle funciona. Combinable con VIP/Frecuente (AND).
- [ ] **P4 Zero state:** con mockData donde nadie está en_riesgo, el chip NO existe en DOM.
- [ ] **Sin regresiones:** Login, Inicio, Agenda, Mensajes, Config siguen funcionando.
- [ ] **Console clean:** sin warnings de React ni errores de TS en dev console.

---

## Self-review

**Spec coverage:**
- ✅ P5 popover desktop → Task 3, 6
- ✅ P5 bottom sheet mobile → Task 3 (isMobile branch)
- ✅ P5 click en wordmark (no solo dot) → Task 5
- ✅ P5 contenido (status + KPIs + actividad + link a BotSettings) → Task 3
- ✅ P5 animaciones floatIn + slideUpSheet → Task 2
- ✅ P5 accessibility (role, aria, Escape, click afuera) → Task 3
- ✅ P4 reglas 30/60d + no-shows → Task 7
- ✅ P4 sort ambient → Task 8
- ✅ P4 tag inline con jerarquía → Task 9
- ✅ P4 chip condicional → Task 10
- ✅ P4 filter combinable → Task 10 Step 3 (AND)
- ✅ NO banner / NO sidebar badge → no hay task que los implemente

**Placeholder scan:** sin TBD/TODO. Un "ajustar si el shape es distinto" en Task 9 Step 1 es una nota contextual sobre la forma de los datos existentes, no un placeholder de lógica.

**Type consistency:**
- `ClientAttentionState` usado consistentemente en tasks 7, 8, 9, 10.
- `AgentActivityItem` / `AgentState` importados desde mockData en Task 3.
- `attentionOnly` naming consistente entre state, filter pipeline, chip.
- `attentionCount` computado una vez, usado en el chip.
